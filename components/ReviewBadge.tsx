import { GoogleRatingBadge } from "@/components/GoogleRatingBadge";

// TODO: Replace placeholder rating/count with live data from Google or your CMS.
export const ReviewBadge = () => {
  return <GoogleRatingBadge rating={4.266} reviewCount={120} />;
};
